export type Shuttle = {
  id: string;
  routeName: string;
  departureTime: string;
  seatsAvailable: number;
};

export type UserProfile = {
  fullName: string;
  email: string;
  company: string;
};
